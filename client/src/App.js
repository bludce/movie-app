import React, { Component, Fragment } from "react";
import LoginPage from "./components/loginPage/loginPage";

const App = ( ) => {
  return (
    <Fragment>
      <LoginPage />
    </Fragment>
    
  );
};

export default App;